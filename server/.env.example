# Database connection (Render/Railway PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/database

# Server
PORT=3001
NODE_ENV=development

# CORS - Frontend URL
FRONTEND_URL=http://localhost:5173

# API Key for tracking endpoints (optional)
API_KEY=your-secret-api-key

# Google Analytics 4 Data API (optional)
# To enable GA4 data on the dashboard:
# 1. Go to Google Cloud Console and create a project
# 2. Enable "Google Analytics Data API"
# 3. Create a Service Account and download JSON credentials
# 4. In GA4 Admin > Property Access Management, add the service account email
# 5. Set the variables below:
GA4_PROPERTY_ID=123456789
# Option A: Path to credentials file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Option B: JSON credentials as string (for hosting platforms like Render/Railway)
GA4_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}
